name: US_Nerve
global_batch_size: 16
img_size: [420, 580]
num_channels: 1
num_classes: 1
ignore_index: 0
class_names: ["Nerves"]

train_dataset:
  _partial_: false
  _target_: data.data.SEGDataset
  root_dir: ${data_dir}/US_Nerve/
  split: train
  img_size : ${img_size}

val_dataset:
  _partial_: false
  _target_: data.data.SEGDataset
  root_dir: ${data_dir}/US_Nerve/
  split: val
  img_size : ${img_size}

test_dataset:
  _partial_: false
  _target_: data.data.SEGDataset
  root_dir: ${data_dir}/US_Nerve/
  split: test
  img_size : ${img_size}
